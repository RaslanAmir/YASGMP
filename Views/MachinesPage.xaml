<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="YasGMP.Views.MachinesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:maui="http://schemas.microsoft.com/dotnet/2021/maui"
    Title="Strojevi / Oprema – YasGMP">

  <ContentPage.Content>
    <VerticalStackLayout Padding="28,20,28,20" Spacing="18"
                         BackgroundColor="{StaticResource YtSurface}">
      <Frame BackgroundColor="{StaticResource YtSurfaceCard}" CornerRadius="14" HasShadow="True" Padding="20"
             BorderColor="{StaticResource YtRowBorder}">

        <VerticalStackLayout Spacing="16">
          <Label Text="Pregled svih strojeva / opreme"
                 FontSize="22" FontAttributes="Bold"
                 TextColor="{StaticResource YtPrimary700}"
                 HorizontalOptions="Center"/>

          <HorizontalStackLayout Spacing="14">
            <Button Text="➕ Dodaj novi stroj"
                    Clicked="OnAddMachineClicked"
                    BackgroundColor="{StaticResource YtPrimary600}"
                    TextColor="White"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Unos novog stroja ili opreme u sustav." />
            <Button Text="✏️ Uredi"
                    Clicked="OnEditMachineClicked"
                    BackgroundColor="{StaticResource YtAccent50}"
                    TextColor="{StaticResource YtAccent700}"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Uređivanje odabranog stroja." />
            <Button Text="🗑️ Briši"
                    Clicked="OnDeleteMachineClicked"
                    BackgroundColor="{StaticResource YtDanger700}"
                    TextColor="White"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Brisanje odabranog stroja iz baze." />
            <Button Text="📄 Dokumenti"
                    Clicked="OnDocumentsClicked"
                    BackgroundColor="{StaticResource YtPrimary50}"
                    TextColor="{StaticResource YtPrimary700}"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Priloženi dokumenti za stroj" />
            <Button Text="🔗 QR"
                    Clicked="OnQrClicked"
                    BackgroundColor="{StaticResource YtPrimary50}"
                    TextColor="{StaticResource YtPrimary700}"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Pregled ili generiranje QR koda" />
            <Button Text="🧩 Komponente"
                    Clicked="OnComponentsClicked"
                    BackgroundColor="{StaticResource YtPrimary50}"
                    TextColor="{StaticResource YtPrimary700}"
                    FontAttributes="Bold"
                    maui:ToolTipProperties.Text="Pregled povezanih komponenti" />
          </HorizontalStackLayout>

          <CollectionView x:Name="MachineListView"
                          ItemsSource="{Binding Machines}"
                          SelectionMode="Single"
                          HeightRequest="440"
                          BackgroundColor="{StaticResource YtSurface}">
            <CollectionView.ItemTemplate>
              <DataTemplate>
                <Frame Margin="0,2,0,2" CornerRadius="8" Padding="10"
                       BackgroundColor="{StaticResource YtSurface}"
                       HasShadow="False"
                       BorderColor="{StaticResource YtRowBorder}">
                  <VerticalStackLayout Spacing="4">
                    <Label Text="{Binding Name}" FontSize="17" FontAttributes="Bold" TextColor="{StaticResource YtOnSurface}" />
                    <Label Text="{Binding Code}" FontSize="13" TextColor="{StaticResource YtOnSurfaceDim}"
                           maui:ToolTipProperties.Text="Interna oznaka stroja" />
                    <Label Text="{Binding Manufacturer}" FontSize="13" TextColor="{StaticResource YtOnSurface}" />
                    <Label Text="{Binding Location}" FontSize="13" TextColor="{StaticResource YtOnSurfaceDim}" />
                    <Label Text="{Binding InstallDate, StringFormat='Instalacija: {0:yyyy-MM-dd}'}"
                           FontSize="12" TextColor="{StaticResource YtOnSurfaceDim}" />
                    <Label Text="{Binding Status}" FontSize="12" TextColor="{StaticResource YtAccent700}"
                           maui:ToolTipProperties.Text="Status (active, maintenance…)" />
                  </VerticalStackLayout>
                </Frame>
              </DataTemplate>
            </CollectionView.ItemTemplate>
          </CollectionView>

          <Label Text="Kliknite na redak za odabir stroja. Moguće je uređivati ili brisati samo odabrani stroj."
                 FontSize="12" TextColor="{StaticResource YtOnSurfaceDim}" HorizontalOptions="Center"/>
        </VerticalStackLayout>

      </Frame>
    </VerticalStackLayout>
  </ContentPage.Content>
</ContentPage>
