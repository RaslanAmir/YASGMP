<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YasGMP.Views.MainPage"
             Title="YasGMP Dashboard">

  <ScrollView>
    <VerticalStackLayout Padding="20" Spacing="28" BackgroundColor="{StaticResource YtSurface}">

      <Label Text="YasGMP – Glavna Kontrolna Ploča"
             FontSize="26" FontAttributes="Bold"
             HorizontalOptions="Center"
             TextColor="{StaticResource YtPrimary}"
             ToolTipProperties.Text="Glavna ploča za sve GMP module" />

      <Label Text="{Binding WelcomeText}"
             FontSize="16"
             TextColor="{StaticResource YtOnSurface}"
             HorizontalOptions="Center"
             Margin="0,8,0,18"
             ToolTipProperties.Text="Trenutno prijavljeni korisnik i uloga" />

      <Frame HasShadow="True" CornerRadius="12" Padding="12" Margin="0,0,0,16"
             BackgroundColor="{StaticResource YtSurfaceCard}">
        <VerticalStackLayout Spacing="14">
          <Button Text="Radni Nalozi"             Command="{Binding OpenWorkOrdersCommand}"  />
          <Button Text="Kalibracije"              Command="{Binding OpenCalibrationsCommand}"/>
          <Button Text="Korisnici"                 Command="{Binding OpenUsersCommand}"      />
          <Button Text="Audit log"                 Command="{Binding OpenAuditLogCommand}"   />
          <Button Text="SOP / Dokumentacija"      Command="{Binding OpenSopCommand}"        />
          <Button Text="Inspekcije"               Command="{Binding OpenInspectionsCommand}"/>
          <Button Text="Dijelovi (Parts)"         Command="{Binding OpenPartsCommand}"      />
          <Button Text="CAPA"                      Command="{Binding OpenCapaCommand}"       />
          <Button Text="Dobavljači i Serviseri"   Command="{Binding OpenSuppliersCommand}"  />
          <Button Text="Izvještaji / Dashboard"   Command="{Binding OpenReportsCommand}"    />
        </VerticalStackLayout>
      </Frame>

      <Label Text="Verzija: 1.0.0   |   Powered by YasTech   |   © 2025"
             FontSize="12"
             HorizontalOptions="Center"
             TextColor="{StaticResource YtOnSurfaceDim}"
             Margin="0,18,0,0"
             ToolTipProperties.Text="Ažuriraj aplikaciju za nove funkcionalnosti" />

    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
