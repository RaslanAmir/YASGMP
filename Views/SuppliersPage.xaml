<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="YasGMP.SuppliersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ui="clr-namespace:YasGMP.UI"
    Title="Dobavljači / Serviseri – YasGMP">

    <ContentPage.Content>
        <VerticalStackLayout Padding="28,20,28,20" Spacing="18" BackgroundColor="#f5f7fa">
            <Frame 
                BackgroundColor="White"
                CornerRadius="14"
                HasShadow="True"
                Padding="20">

                <VerticalStackLayout Spacing="16">
                    <Label 
                        Text="Pregled svih dobavljača, servisera i laboratorija"
                        FontSize="22"
                        FontAttributes="Bold"
                        TextColor="#2962ff"
                        HorizontalOptions="Center"/>

                    <HorizontalStackLayout Spacing="14">
                        <Button 
                            Text="➕ Dodaj novog dobavljača"
                            Clicked="OnAddSupplierClicked"
                            BackgroundColor="#e3f2fd"
                            TextColor="#1565c0"
                            FontAttributes="Bold"
                            ui:ToolTipProperties.Text="Unos novog dobavljača ili servisera." />

                        <Button 
                            Text="✏️ Uredi"
                            Clicked="OnEditSupplierClicked"
                            BackgroundColor="#fffde7"
                            TextColor="#f9a825"
                            FontAttributes="Bold"
                            ui:ToolTipProperties.Text="Uređivanje odabranog dobavljača." />

                        <Button 
                            Text="🗑️ Briši"
                            Clicked="OnDeleteSupplierClicked"
                            BackgroundColor="#ffebee"
                            TextColor="#c62828"
                            FontAttributes="Bold"
                            ui:ToolTipProperties.Text="Brisanje odabranog dobavljača iz baze." />
                    </HorizontalStackLayout>

                    <CollectionView 
                        x:Name="SupplierListView"
                        ItemsSource="{Binding Suppliers}"
                        SelectionMode="Single"
                        HeightRequest="440"
                        BackgroundColor="#f5f7fa">

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame 
                                    Margin="0,2,0,2"
                                    CornerRadius="8"
                                    Padding="10"
                                    BackgroundColor="#f8fafc"
                                    HasShadow="False"
                                    BorderColor="#cfd8dc">

                                    <VerticalStackLayout Spacing="3">
                                        <Label 
                                            Text="{Binding Name}"
                                            FontSize="17"
                                            FontAttributes="Bold"
                                            TextColor="#263238" />
                                        <Label 
                                            Text="{Binding SupplierType}"
                                            FontSize="13"
                                            TextColor="#00796b"
                                            ui:ToolTipProperties.Text="Tip dobavljača (dijelovi, servis, laboratorij...)" />
                                        <Label 
                                            Text="{Binding VatNumber}"
                                            FontSize="12"
                                            TextColor="#455a64"
                                            ui:ToolTipProperties.Text="OIB / porezni broj" />
                                        <Label 
                                            Text="{Binding Address}"
                                            FontSize="12"
                                            TextColor="#78909c" />
                                        <Label 
                                            Text="{Binding City}"
                                            FontSize="12"
                                            TextColor="#607d8b" />
                                        <Label 
                                            Text="{Binding Country}"
                                            FontSize="12"
                                            TextColor="#b0bec5" />
                                        <Label 
                                            Text="{Binding Email}"
                                            FontSize="12"
                                            TextColor="#2962ff" />
                                        <Label 
                                            Text="{Binding Phone}"
                                            FontSize="12"
                                            TextColor="#bdbdbd" />
                                        <Label 
                                            Text="{Binding Website}"
                                            FontSize="12"
                                            TextColor="#5d4037" />
                                        <Label 
                                            Text="{Binding ContractFile}"
                                            FontSize="12"
                                            TextColor="#d84315"
                                            ui:ToolTipProperties.Text="Putanja do PDF ugovora/certifikata" />
                                        <Label 
                                            Text="{Binding Notes}"
                                            FontSize="12"
                                            TextColor="#ef6c00"
                                            ui:ToolTipProperties.Text="Posebne napomene (GMP, partner status, validacija...)" />
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <Label
                        Text="Kliknite na redak za odabir dobavljača. Moguće je uređivati ili brisati samo odabranog."
                        FontSize="12"
                        TextColor="#bdbdbd"
                        HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
