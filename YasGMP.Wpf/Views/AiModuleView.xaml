<UserControl x:Class="YasGMP.Wpf.Views.AiModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="AI Assistant" FontSize="18" FontWeight="Bold" />
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBlock Text="{Binding Response}" TextWrapping="Wrap"/>
        </ScrollViewer>
        <DockPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBox Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}" Height="60" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
            <Button Content="Ask" Command="{Binding AskCommand}" Margin="8,0,0,0" Width="100" />
        </DockPanel>
    </Grid>
</UserControl>

