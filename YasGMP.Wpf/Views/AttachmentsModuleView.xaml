<UserControl x:Class="YasGMP.Wpf.Views.AttachmentsModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:YasGMP.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ItemsControl ItemsSource="{Binding Toolbar}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Content="{Binding Caption}"
                                      Command="{Binding Command}"
                                      IsChecked="{Binding IsChecked}"
                                      Margin="0,0,4,0" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator />
            <Button Content="{DynamicResource Module.Toolbar.Button.Cfl.Content}" Command="{Binding ShowCflCommand}" ToolTipService.ToolTip="{DynamicResource Module.Toolbar.Button.Cfl.ToolTip}" AutomationProperties.Name="{DynamicResource Module.Toolbar.Button.Cfl.AutomationName}" AutomationProperties.AutomationId="{DynamicResource Module.Toolbar.Button.Cfl.AutomationId}"/>
            <controls:GoldenArrowButton
                Command="{Binding GoldenArrowCommand}"
                Margin="8,0,0,0"
                ToolTipKey="Module.Toolbar.Button.GoldenArrow.ToolTip"
                AutomationNameKey="Module.Toolbar.Button.GoldenArrow.AutomationName"
                AutomationIdKey="Module.Toolbar.Button.GoldenArrow.AutomationId" />
        </ToolBar>
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
                <TextBlock Text="{DynamicResource Dashboard.Search.Label}" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Width="220" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
            <DataGrid Grid.Row="1"
                      Grid.Column="0"
                      ItemsSource="{Binding RecordsView}"
                      SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Code.Header}" Binding="{Binding Code}" Width="120" />
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Title.Header}" Binding="{Binding Title}" Width="2*" />
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Status.Header}" Binding="{Binding Status}" Width="140" />
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Description.Header}" Binding="{Binding Description}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasAttachmentWorkflow}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasAttachmentWorkflow}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="{DynamicResource Module.Attachments.Content.PickFiles}"
                            Command="{Binding UploadCommand}"
                            Margin="0,0,8,0" 
                           ToolTipService.ToolTip="{DynamicResource Module.Attachments.ToolTip.PickFiles}"
                           AutomationProperties.Name="{DynamicResource Module.Attachments.AutomationName.PickFiles}"
                           AutomationProperties.AutomationId="{DynamicResource Module.Attachments.Button.PickFiles.AutomationId}"/>
                    <Button Content="{DynamicResource Attachments.Toolbar.Command.Download.Content}"
                            Command="{Binding DownloadCommand}"
                            CommandParameter="{Binding SelectedAttachment}"
                            Margin="0,0,8,0" 
                           ToolTipService.ToolTip="{DynamicResource Attachments.Toolbar.Command.Download.ToolTip}"
                           AutomationProperties.Name="{DynamicResource Attachments.Toolbar.Command.Download.AutomationName}"
                           AutomationProperties.AutomationId="{DynamicResource Attachments.Toolbar.Command.Download.AutomationId}"/>
                    <Button Content="{DynamicResource Attachments.Toolbar.Command.Delete.Content}"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding SelectedAttachment}" 
                           ToolTipService.ToolTip="{DynamicResource Attachments.Toolbar.Command.Delete.ToolTip}"
                           AutomationProperties.Name="{DynamicResource Attachments.Toolbar.Command.Delete.AutomationName}"
                           AutomationProperties.AutomationId="{DynamicResource Attachments.Toolbar.Command.Delete.AutomationId}"/>
                </StackPanel>
                <Border Grid.Row="1"
                        Padding="12"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
                    <Border.DataContext>
                        <Binding Path="SelectedAttachment" />
                    </Border.DataContext>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                        <TextBlock Text="{DynamicResource Module.Attachments.Text.SelectAnAttachmentToViewDetails}" FontStyle="Italic" Foreground="Gray">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <StackPanel Margin="0,0,0,16">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource Module.Attachments.Text.AttachmentDetails}" FontWeight="Bold" FontSize="16" Margin="0,0,0,12" />
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.DisplayName}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="0"
                                         Grid.Column="1"
                                         Text="{Binding DisplayName}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.FileName}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="1"
                                         Grid.Column="1"
                                         Text="{Binding FileName}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.Status}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="2"
                                         Grid.Column="1"
                                         Text="{Binding Status}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.Entity}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="3"
                                         Grid.Column="1"
                                         Text="{Binding EntityDisplayName}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.FileType}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="4"
                                         Grid.Column="1"
                                         Text="{Binding FileType}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="5" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.FileSize}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="5"
                                         Grid.Column="1"
                                         Text="{Binding FileSizeDisplay}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="6" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.SHA256}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="6"
                                         Grid.Column="1"
                                         Text="{Binding Sha256}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="7" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.Notes}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="7"
                                         Grid.Column="1"
                                         Text="{Binding Notes}"
                                         IsReadOnly="True"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="60"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="8" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.RetentionPolicy}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="8"
                                         Grid.Column="1"
                                         Text="{Binding RetentionPolicyName}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="9" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.RetainUntil}" Margin="0,0,8,4" />
                                <TextBox Grid.Row="9"
                                         Grid.Column="1"
                                         Text="{Binding RetainUntil}"
                                         IsReadOnly="True"
                                         Margin="0,0,0,4" />
                                <TextBlock Grid.Row="10" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.RetentionFlags}" Margin="0,0,8,4" />
                                <StackPanel Grid.Row="10" Grid.Column="1" Orientation="Vertical" Margin="0,0,0,4">
                                    <CheckBox Content="{DynamicResource Module.Attachments.Content.LegalHold}"
                                              IsChecked="{Binding RetentionLegalHold}"
                                              IsEnabled="False"
                                              Margin="0,0,0,4" />
                                    <CheckBox Content="{DynamicResource Module.Attachments.Content.ReviewRequired}"
                                              IsChecked="{Binding RetentionReviewRequired}"
                                              IsEnabled="False"
                                              Margin="0,0,0,4" />
                                    <TextBlock Text="{DynamicResource Module.Attachments.Text.RetentionNotes}" FontWeight="SemiBold" Margin="0,8,0,4" />
                                    <TextBox Text="{Binding RetentionNotes}"
                                             IsReadOnly="True"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             MinHeight="40" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <Separator />
                        <TextBlock Text="{DynamicResource Module.Attachments.Text.StagedUploads}" FontWeight="Bold" FontSize="16" Margin="0,12,0,8" />
                        <ItemsControl ItemsSource="{Binding DataContext.StagedUploads, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gainsboro" BorderThickness="1" Padding="8" Margin="0,0,0,8" CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.FileName}" Margin="0,0,8,4" VerticalAlignment="Center" />
                                            <TextBox Grid.Row="0"
                                                     Grid.Column="1"
                                                     Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.ContentType}" Margin="0,0,8,4" VerticalAlignment="Center" />
                                            <TextBox Grid.Row="1"
                                                     Grid.Column="1"
                                                     Text="{Binding ContentType, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.EntityType}" Margin="0,0,8,4" VerticalAlignment="Center" />
                                            <TextBox Grid.Row="2"
                                                     Grid.Column="1"
                                                     Text="{Binding EntityType, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.EntityID}" Margin="0,0,8,4" VerticalAlignment="Center" />
                                            <TextBox Grid.Row="3"
                                                     Grid.Column="1"
                                                     Text="{Binding EntityId, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource Module.Attachments.Text.Notes}" Margin="0,0,8,0" VerticalAlignment="Top" />
                                            <TextBox Grid.Row="4"
                                                     Grid.Column="1"
                                                     Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                                     AcceptsReturn="True"
                                                     TextWrapping="Wrap"
                                                     MinHeight="40" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="{DynamicResource Module.Attachments.Text.NoFilesStagedForUploadUseUploadToAddFilesBeforeSaving}"
                                   FontStyle="Italic"
                                   Foreground="Gray"
                                   Margin="0,8,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.HasStagedUploads, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            <StackPanel Grid.Row="2"
                        Grid.ColumnSpan="2"
                        Margin="0,8,0,0">
                <ItemsControl ItemsSource="{Binding ValidationMessages}"
                              Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="{DynamicResource Module.Assets.Text.Value}" Foreground="DarkRed" Margin="0,0,4,0" />
                                <TextBlock Text="{Binding}" Foreground="DarkRed" TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="{Binding StatusMessage}"
                           FontStyle="Italic"
                           Margin="0,4,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Gray" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasValidationErrors}" Value="True">
                                    <Setter Property="Foreground" Value="Firebrick" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
            <Border Grid.Row="0"
                    Grid.Column="0"
                    Grid.RowSpan="3"
                    Grid.ColumnSpan="2"
                    Background="#80FFFFFF"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                    IsHitTestVisible="True"
                    Panel.ZIndex="100">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Orientation="Vertical"
                            Margin="24">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20" />
                    <TextBlock Text="{DynamicResource Module.Attachments.Text.Processing}"
                               Margin="0,12,0,0"
                               HorizontalAlignment="Center"
                               Foreground="Gray"
                               FontStyle="Italic" />
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
