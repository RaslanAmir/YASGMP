<UserControl x:Class="YasGMP.Wpf.Views.AuditLogDocument"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:YasGMP.Wpf.ViewModels.Modules"
             xmlns:controls="clr-namespace:YasGMP.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=vm:AuditLogDocumentViewModel}"
             AutomationProperties.Name="{DynamicResource Module_AuditTrail_Title}"
             ToolTipService.ToolTip="{DynamicResource Module_AuditTrail_Tooltip}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="BusyAwareToolbarButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Padding" Value="12,4" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ItemsControl ItemsSource="{Binding Toolbar}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Content="{Binding Caption}"
                                      Command="{Binding Command}"
                                      IsChecked="{Binding IsChecked}"
                                      Margin="0,0,4,0" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator />
            <Button Content="CFL"
                    Command="{Binding ShowCflCommand}"
                    AutomationProperties.AutomationId="CflButton" />
            <controls:GoldenArrowButton Command="{Binding GoldenArrowCommand}"
                                       Margin="8,0,0,0"
                                       AutomationProperties.AutomationId="GoldenArrowButton" />
            <Separator />
            <Button Content="Apply Filters"
                    Command="{Binding ApplyFilterCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" />
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" />
            <Button Content="Export CSV"
                    Command="{Binding ExportCsvCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" />
            <Button Content="Export Excel"
                    Command="{Binding ExportXlsxCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" />
            <Button Content="Export PDF"
                    Command="{Binding ExportPdfCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" />
        </ToolBar>

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <WrapPanel Margin="0,0,0,8">
                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label_User}" FontWeight="SemiBold" />
                        <TextBox Text="{Binding AuditLog.FilterUserIdText, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label_Entity}" FontWeight="SemiBold" />
                        <TextBox Text="{Binding AuditLog.FilterEntity, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label_Action}" FontWeight="SemiBold" />
                        <ComboBox ItemsSource="{Binding AuditLog.ActionTypes}"
                                  SelectedItem="{Binding AuditLog.SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label_From}" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding AuditLog.FilterFrom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label_To}" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding AuditLog.FilterTo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </WrapPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource Label_Search}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Grid.Column="1"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2"
                            Content="Apply"
                            Command="{Binding ApplyFilterCommand}"
                            Margin="12,0,0,0"
                            Padding="12,4"
                            Style="{StaticResource BusyAwareToolbarButtonStyle}" />
                    <Button Grid.Column="3"
                            Content="Refresh"
                            Command="{Binding RefreshCommand}"
                            Margin="12,0,0,0"
                            Padding="12,4"
                            Style="{StaticResource BusyAwareToolbarButtonStyle}" />
                </Grid>
            </StackPanel>

            <Grid Grid.Row="1">
                <DataGrid ItemsSource="{Binding RecordsView}"
                          SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding InspectorFields[0].Value}" Width="160" />
                        <DataGridTextColumn Header="User" Binding="{Binding InspectorFields[1].Value}" Width="180" />
                        <DataGridTextColumn Header="Entity" Binding="{Binding InspectorFields[2].Value}" Width="220" />
                        <DataGridTextColumn Header="Action" Binding="{Binding InspectorFields[3].Value}" Width="120" />
                        <DataGridTextColumn Header="Device" Binding="{Binding InspectorFields[4].Value}" Width="200" />
                        <DataGridTextColumn Header="IP Address" Binding="{Binding InspectorFields[5].Value}" Width="160" />
                        <DataGridTextColumn Header="Description" Binding="{Binding InspectorFields[6].Value}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Border Background="#80FFFFFF"
                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                        IsHitTestVisible="True">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Vertical"
                                Margin="24">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="20" />
                        <TextBlock Text="{DynamicResource Label_Loading}"
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center"
                                   Foreground="Gray"
                                   FontStyle="Italic" />
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Grid.Row="2"
                       Text="{Binding StatusMessage}"
                       FontStyle="Italic"
                       Margin="0,8,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="Gray" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                <Setter Property="Foreground" Value="Firebrick" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </DockPanel>
</UserControl>
