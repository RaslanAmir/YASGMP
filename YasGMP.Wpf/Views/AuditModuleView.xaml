<UserControl x:Class="YasGMP.Wpf.Views.AuditModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:YasGMP.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="BusyAwareToolbarButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Padding" Value="12,4" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ItemsControl ItemsSource="{Binding Toolbar}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Content="{Binding Caption}"
                                      Command="{Binding Command}"
                                      IsChecked="{Binding IsChecked}"
                                      Margin="0,0,4,0" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator />
            <Button Content="{DynamicResource Module.Toolbar.Button.Cfl.Content}" Command="{Binding ShowCflCommand}" ToolTipService.ToolTip="{DynamicResource Module.Toolbar.Button.Cfl.ToolTip}" AutomationProperties.Name="{DynamicResource Module.Toolbar.Button.Cfl.AutomationName}" AutomationProperties.AutomationId="{DynamicResource Module.Toolbar.Button.Cfl.AutomationId}"/>
            <controls:GoldenArrowButton
                Command="{Binding GoldenArrowCommand}"
                Margin="8,0,0,0"
                ToolTipKey="Module.Toolbar.Button.GoldenArrow.ToolTip"
                AutomationNameKey="Module.Toolbar.Button.GoldenArrow.AutomationName"
                AutomationIdKey="Module.Toolbar.Button.GoldenArrow.AutomationId" />
            <Separator />
            <Button Content="{DynamicResource Audit.Toolbar.Command.ExportPdf.Content}"
                    Command="{Binding ExportToPdfCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" 
                   ToolTipService.ToolTip="{DynamicResource Audit.Toolbar.Command.ExportPdf.ToolTip}"
                   AutomationProperties.Name="{DynamicResource Audit.Toolbar.Command.ExportPdf.AutomationName}"
                   AutomationProperties.AutomationId="{DynamicResource Audit.Toolbar.Command.ExportPdf.AutomationId}"/>
            <Button Content="{DynamicResource Audit.Toolbar.Command.ExportExcel.Content}"
                    Command="{Binding ExportToExcelCommand}"
                    Style="{StaticResource BusyAwareToolbarButtonStyle}" 
                   ToolTipService.ToolTip="{DynamicResource Audit.Toolbar.Command.ExportExcel.ToolTip}"
                   AutomationProperties.Name="{DynamicResource Audit.Toolbar.Command.ExportExcel.AutomationName}"
                   AutomationProperties.AutomationId="{DynamicResource Audit.Toolbar.Command.ExportExcel.AutomationId}"/>
        </ToolBar>
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <WrapPanel Margin="0,0,0,8">
                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Module.ApiAudit.Header.User}" FontWeight="SemiBold" />
                        <TextBox Text="{Binding FilterUser, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Module.Audit.Header.Entity}" FontWeight="SemiBold" />
                        <TextBox Text="{Binding FilterEntity, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Module.ApiAudit.Header.Action}" FontWeight="SemiBold" />
                        <ComboBox ItemsSource="{Binding ActionOptions}"
                                  SelectedItem="{Binding SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Module.ApiAudit.Text.From}" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding FilterFrom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue={x:Null}, FallbackValue={x:Null}}" />
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Module.ApiAudit.Text.To}" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding FilterTo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue={x:Null}, FallbackValue={x:Null}}" />
                    </StackPanel>
                </WrapPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource Dashboard.Search.Label}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2"
                            Content="{DynamicResource AuditDashboard.Toolbar.Command.ApplyFilters.Content}"
                            Command="{Binding RefreshCommand}"
                            Margin="12,0,0,0"
                            Padding="12,4" 
                           ToolTipService.ToolTip="{DynamicResource AuditDashboard.Toolbar.Command.ApplyFilters.ToolTip}"
                           AutomationProperties.Name="{DynamicResource AuditDashboard.Toolbar.Command.ApplyFilters.AutomationName}"
                           AutomationProperties.AutomationId="{DynamicResource AuditDashboard.Toolbar.Command.ApplyFilters.AutomationId}"/>
                </Grid>
            </StackPanel>
            <Grid Grid.Row="1">
                <DataGrid ItemsSource="{Binding RecordsView}"
                          SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Module.ApiAudit.Header.Timestamp}" Binding="{Binding InspectorFields[0].Value}" Width="160" />
                        <DataGridTextColumn Header="{DynamicResource Module.ApiAudit.Header.User}" Binding="{Binding InspectorFields[1].Value}" Width="200" />
                        <DataGridTextColumn Header="{DynamicResource Module.Audit.Header.Entity}" Binding="{Binding Title}" Width="200" />
                        <DataGridTextColumn Header="{DynamicResource Module.ApiAudit.Header.Action}" Binding="{Binding Code}" Width="120" />
                        <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Status.Header}" Binding="{Binding Status}" Width="120" />
                        <DataGridTextColumn Header="{DynamicResource Module.ApiAudit.Header.IPAddress}" Binding="{Binding InspectorFields[4].Value}" Width="160" />
                        <DataGridTextColumn Header="{DynamicResource Module.Audit.Header.Device}" Binding="{Binding InspectorFields[5].Value}" Width="2*" />
                        <DataGridTextColumn Header="{DynamicResource Module.Audit.Header.DigitalSignature}" Binding="{Binding InspectorFields[7].Value}" Width="200" />
                        <DataGridTextColumn Header="{DynamicResource Module.Audit.Header.SignatureHash}" Binding="{Binding InspectorFields[8].Value}" Width="200" />
                        <DataGridTextColumn Header="{DynamicResource Module.Audit.Header.Reason}" Binding="{Binding Description}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
                <Border Background="#80FFFFFF"
                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                        IsHitTestVisible="True">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Vertical"
                                Margin="24">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="20" />
                        <TextBlock Text="{DynamicResource Module.ApiAudit.Text.Loading}"
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center"
                                   Foreground="Gray"
                                   FontStyle="Italic" />
                    </StackPanel>
                </Border>
            </Grid>
            <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" FontStyle="Italic" Margin="0,8,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="Gray" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                <Setter Property="Foreground" Value="Firebrick" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </DockPanel>
</UserControl>
