<UserControl x:Class="YasGMP.Wpf.Views.CockpitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200"
             d:DesignWidth="600">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource Label_ProductionKpis}"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,8" />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Metrics}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="4"
                            Background="{Binding AccentHex}"
                            Margin="0,0,8,0"
                            Padding="12"
                            MinWidth="120">
                        <StackPanel>
                            <TextBlock Text="{Binding Label}" Foreground="White" FontWeight="Bold" />
                            <TextBlock Text="{Binding FormattedValue}" Foreground="White" FontSize="18" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="{DynamicResource Label_Notices}" FontWeight="Bold" Margin="0,0,0,4" />
            <ItemsControl ItemsSource="{Binding Notices}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="0,0,0,4">
                            <Ellipse Width="6" Height="6" Fill="#007ACC" Margin="0,6,6,0" />
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
