<UserControl x:Class="YasGMP.Wpf.Views.QualificationsModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:YasGMP.Wpf.Controls"
             xmlns:vm="clr-namespace:YasGMP.Wpf.ViewModels.Modules"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="900"
             d:DataContext="{d:DesignInstance vm:QualificationsModuleViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/YasGMP.Wpf;component/Resources/ModuleToolbarResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ItemsControl ItemsSource="{Binding Toolbar}"
                          ItemTemplate="{StaticResource ModuleToolbarToggleButtonTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <Separator />
            <Button Content="{DynamicResource Module.Toolbar.Button.Cfl.Content}"
                    Command="{Binding ShowCflCommand}"
                    ToolTipService.ToolTip="{DynamicResource Module.Toolbar.Button.Cfl.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Toolbar.Button.Cfl.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Toolbar.Button.Cfl.AutomationId}" />
            <controls:GoldenArrowButton Command="{Binding GoldenArrowCommand}"
                                       Margin="8,0,0,0"
                                       ToolTipKey="Module.Toolbar.Button.GoldenArrow.ToolTip"
                                       AutomationNameKey="Module.Toolbar.Button.GoldenArrow.AutomationName"
                                       AutomationIdKey="Module.Toolbar.Button.GoldenArrow.AutomationId" />
            <Separator />
            <Button Content="{DynamicResource Module.Qualifications.Toolbar.Add.Content}"
                    Command="{Binding AddCommand}"
                    Margin="8,0,0,0"
                    ToolTipService.ToolTip="{DynamicResource Module.Qualifications.Toolbar.Add.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Qualifications.Toolbar.Add.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Toolbar.Add.AutomationId}" />
            <Button Content="{DynamicResource Module.Qualifications.Toolbar.Update.Content}"
                    Command="{Binding UpdateCommand}"
                    Margin="8,0,0,0"
                    ToolTipService.ToolTip="{DynamicResource Module.Qualifications.Toolbar.Update.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Qualifications.Toolbar.Update.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Toolbar.Update.AutomationId}" />
            <Button Content="{DynamicResource Module.Qualifications.Toolbar.Delete.Content}"
                    Command="{Binding DeleteCommand}"
                    Margin="8,0,0,0"
                    ToolTipService.ToolTip="{DynamicResource Module.Qualifications.Toolbar.Delete.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Qualifications.Toolbar.Delete.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Toolbar.Delete.AutomationId}" />
            <Button Content="{DynamicResource Module.Qualifications.Toolbar.Rollback.Content}"
                    Command="{Binding RollbackCommand}"
                    Margin="8,0,0,0"
                    ToolTipService.ToolTip="{DynamicResource Module.Qualifications.Toolbar.Rollback.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Qualifications.Toolbar.Rollback.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Toolbar.Rollback.AutomationId}" />
            <Button Content="{DynamicResource Module.Qualifications.Toolbar.Export.Content}"
                    Command="{Binding ExportCommand}"
                    Margin="8,0,0,0"
                    ToolTipService.ToolTip="{DynamicResource Module.Qualifications.Toolbar.Export.ToolTip}"
                    AutomationProperties.Name="{DynamicResource Module.Qualifications.Toolbar.Export.AutomationName}"
                    AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Toolbar.Export.AutomationId}" />
        </ToolBar>

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Row="0"
                       Grid.ColumnSpan="2"
                       Margin="0,0,0,8">
                <TextBlock Text="{DynamicResource Dashboard.Search.Label}"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0" />
                <TextBox Width="240"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTipService.ToolTip="{DynamicResource Dashboard.Search.Label}"
                         AutomationProperties.Name="{DynamicResource Dashboard.Search.Label}"
                         AutomationProperties.AutomationId="{DynamicResource Module.Qualifications.Search.AutomationId}" />
            </DockPanel>

            <StackPanel Grid.Row="1"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Margin="0,0,0,12">
                <StackPanel Orientation="Vertical" Margin="0,0,12,0">
                    <TextBlock Text="{DynamicResource Module.Qualifications.Filter.Status}" />
                    <ComboBox Width="180"
                              ItemsSource="{Binding StatusOptions}"
                              SelectedItem="{Binding StatusFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="0,0,12,0">
                    <TextBlock Text="{DynamicResource Module.Qualifications.Filter.Type}" />
                    <ComboBox Width="180"
                              ItemsSource="{Binding TypeOptions}"
                              SelectedItem="{Binding TypeFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </StackPanel>

            <DataGrid Grid.Row="2"
                      Grid.Column="0"
                      ItemsSource="{Binding RecordsView}"
                      SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Code.Header}"
                                        Binding="{Binding Code}"
                                        Width="120" />
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Title.Header}"
                                        Binding="{Binding Title}"
                                        Width="2*" />
                    <DataGridTextColumn Header="{DynamicResource Dashboard.Grid.Column.Status.Header}"
                                        Binding="{Binding Status}"
                                        Width="140" />
                    <DataGridTextColumn Header="{DynamicResource Module.Qualifications.Grid.Column.Equipment}"
                                        Binding="{Binding InspectorFields[0].Value}"
                                        Width="160" />
                    <DataGridTextColumn Header="{DynamicResource Module.Qualifications.Grid.Column.Type}"
                                        Binding="{Binding InspectorFields[1].Value}"
                                        Width="120" />
                    <DataGridTextColumn Header="{DynamicResource Module.Qualifications.Grid.Column.DueDate}"
                                        Binding="{Binding InspectorFields[4].Value}"
                                        Width="130" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="2"
                    Grid.Column="1"
                    Padding="12"
                    Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
                    BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                    BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Module.Qualifications.Inspector.Header}"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   Margin="0,0,0,12" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Dashboard.Grid.Column.Code.Header}" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.Code, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.Type}" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.Type, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.Equipment}" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.EquipmentName, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.Certificate}" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.CertificateNumber, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.EffectiveDate}" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.Date, StringFormat=d, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.DueDate}" />
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.ExpiryDate, StringFormat=d, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.Status}" />
                            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding SelectedRecord.Status, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,8,6" Text="{DynamicResource Module.Qualifications.Field.QualifiedBy}" />
                            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.QualifiedBy.FullName, TargetNullValue=}" IsReadOnly="True" />

                            <TextBlock Grid.Row="8" Grid.Column="0" Margin="0,0,8,0" Text="{DynamicResource Module.Qualifications.Field.ApprovedBy}" />
                            <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Qualifications.SelectedQualification.ApprovedBy.FullName, TargetNullValue=}" IsReadOnly="True" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
