<UserControl x:Class="YasGMP.Wpf.Views.ReportsModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <DockPanel>
        <DockPanel.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="/YasGMP.Wpf;component/Resources/ModuleToolbarResources.xaml" />
                </ResourceDictionary.MergedDictionaries>
                <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            </ResourceDictionary>
        </DockPanel.Resources>
        <ToolBar DockPanel.Dock="Top">
            <ItemsControl ItemsSource="{Binding Toolbar}"
                          ItemTemplate="{StaticResource ModuleToolbarToggleButtonTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <Separator />
            <Button Content="CFL" Command="{Binding ShowCflCommand}" />
        </ToolBar>

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Parameters Pane -->
            <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Search" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Column="2" Text="From" VerticalAlignment="Center" Margin="16,0,8,0" />
                <DatePicker Grid.Column="3" SelectedDate="{Binding FromDate}" />
                <TextBlock Grid.Column="4" Text="To" VerticalAlignment="Center" Margin="16,0,8,0" />
                <DatePicker Grid.Column="5" SelectedDate="{Binding ToDate}" />
            </Grid>

            <DataGrid Grid.Row="1" Grid.Column="0"
                      ItemsSource="{Binding RecordsView}"
                      SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Title}" Header="Title" Width="2*" />
                    <DataGridTextColumn Binding="{Binding Code}" Header="Code" Width="*" />
                    <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="*" />
                    <DataGridTextColumn Binding="{Binding Description}" Header="Description" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="1" Grid.Column="1" Padding="8" BorderBrush="Gainsboro" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Report Details" FontWeight="Bold" FontSize="16" />
                    <ItemsControl ItemsSource="{Binding SelectedRecord.InspectorFields}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel LastChildFill="True" Margin="0,2,0,2">
                                    <TextBlock Text="{Binding Name}" Width="120" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Value}" TextWrapping="Wrap" />
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic" VerticalAlignment="Center" />
                <ProgressBar Width="200" Height="16" Minimum="0" Maximum="100" Value="{Binding ExportProgress, Mode=OneWay}" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="12,0,0,0" />
                <Button Content="Cancel" Command="{Binding CancelExportCommand}" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="12,0,0,0" />
                <Button Content="Save Preset" Command="{Binding SavePresetCommand}" Margin="12,0,0,0" />
                <Button Content="Load Preset" Command="{Binding LoadPresetCommand}" Margin="12,0,0,0" />
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
