<UserControl x:Class="YasGMP.Wpf.Views.RollbackPreviewDocument"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:YasGMP.Wpf.ViewModels.Modules"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:RollbackPreviewDocumentViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <Style x:Key="JsonTextBoxStyle" TargetType="TextBox">
                <Setter Property="FontFamily" Value="Consolas" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="AcceptsReturn" Value="True" />
                <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
                <Setter Property="TextWrapping" Value="NoWrap" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Header}" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Summary}" Foreground="Gray" Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <WrapPanel Grid.Column="0" ItemHeight="60">
                <StackPanel Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource Audit.Rollback.Label.Entity}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding EntityDisplay}" />
                </StackPanel>
                <StackPanel Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource Audit.Rollback.Label.User}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding UserDisplay}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{DynamicResource Audit.Rollback.Label.Timestamp}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding TimestampDisplay}" />
                </StackPanel>
            </WrapPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="{DynamicResource Audit.Rollback.Label.Signature}" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <Ellipse Width="12" Height="12" Fill="{Binding SignatureBrush}" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding SignatureStatus}" Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Text="{Binding SignatureHash}" FontFamily="Consolas" Foreground="Gray" Margin="0,6,0,0"
                           ToolTip="{Binding SignatureHash}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{DynamicResource Audit.Rollback.Label.OldJson}" FontWeight="SemiBold" />
                <Border BorderBrush="#33000000" BorderThickness="1" Margin="0,4,0,0" Background="#0F000000">
                    <TextBox Style="{StaticResource JsonTextBoxStyle}" Text="{Binding OldJson}" />
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                <TextBlock Text="{DynamicResource Audit.Rollback.Label.NewJson}" FontWeight="SemiBold" />
                <Border BorderBrush="#33000000" BorderThickness="1" Margin="0,4,0,0" Background="#0F000000">
                    <TextBox Style="{StaticResource JsonTextBoxStyle}" Text="{Binding NewJson}" />
                </Border>
            </StackPanel>

            <Border Background="#88FFFFFF"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                    IsHitTestVisible="True">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="24">
                    <ProgressBar Width="200" Height="16" IsIndeterminate="True" />
                    <TextBlock Text="{DynamicResource Audit.Rollback.Status.Submitting}" Margin="0,12,0,0" FontStyle="Italic" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic" Foreground="Gray" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Audit.Rollback.Button.Rollback}" Command="{Binding RollbackCommand}"
                        Margin="0,0,8,0" Padding="16,6"
                        ToolTipService.ToolTip="{DynamicResource Audit.Rollback.Button.Rollback.ToolTip}" />
                <Button Content="{DynamicResource Audit.Rollback.Button.Close}" Command="{Binding CloseCommand}"
                        Padding="16,6"
                        ToolTipService.ToolTip="{DynamicResource Audit.Rollback.Button.Close.ToolTip}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
